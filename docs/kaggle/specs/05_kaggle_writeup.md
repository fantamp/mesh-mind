# Спецификация 05: Написание Kaggle Writeup для Enterprise Agents Track

## Обязательное чтение
- **ВАЖНО:** Изучите **Google ADK Documentation:** https://google.github.io/adk-docs/ Требуется глубокое понимание: Multi-agent systems, Tools, Sessions, Evaluation.
- Важно изучить документацию проекта:
    - docs/architecture
    - docs/requirements

## Цель
Создать 2 writeup файла для Kaggle submission:
- `kaggle/writeup.ru.md` — русский (ведущий), макс 1400 слов
- `kaggle/writeup.en.md` — английский перевод, макс 1400 слов

**Аудитория:** Технические судьи Kaggle с AI/ML background.
**Задача:** Демонстрация глубокого понимания и применения ADK концептов.

## Структура Writeup

### 1. Problem Statement (10-15%)
- Проблема: Информационная перегрузка в Telegram чатах
- Сложность ручного поиска/суммаризации
- Необходимость автоматизации

### 2. Solution Overview (15-20%)
- Mesh Mind — мульти-агентная система для анализа чатов
- Три агента, координируемых Orchestrator
- **Ключевое:** Объяснить **почему агентный подход**, а не монолитная LLM

### 3. Architecture & ADK Concepts (60-70%) 🔥 ГЛАВНАЯ СЕКЦИЯ

#### 3.1 Multi-Agent System (обязательно)
**Orchestrator** (`ai_core/agents/orchestrator/agent.py`):
- LLM-driven delegation через `sub_agents` (очень важно объяснить такой выбор!)
- Silent Mode для предотвращения спама
- Преимущество: Избежание brittle keyword matching

**Chat Summarizer** (`ai_core/agents/chat_summarizer/agent.py`):
- Pipeline: fetch_messages → summarizer_agent
- Stateless design

**Chat Observer** (`ai_core/agents/chat_observer/agent.py`):
- Фильтрация: author, time, keywords
- Stateful через sessions

#### 3.2 Custom Tools (обязательно)
**Tool `fetch_messages`** (`ai_core/tools/messages.py`):
- Параметры: `chat_id`, `limit`, `since`, `author_id`, `author_nick`, `contains`
- Преимущество: Гибкая фильтрация vs полный скан БД

#### 3.3 Sessions & Memory (обязательно)
**InMemorySessionService** (`ai_core/common/adk.py`, `run_agent_sync`):
- Паттерн: `session_id = chat_id` для Orchestrator (stateful). Тут важно пояснить, что все агенты запускаемые из оркестратора будут statful.
- Паттерн: `session_id = UUID` для Summarizer если запускается отдельной функцией (stateless). Важно пояснить зачем нужен такой режим.
- Balance между context preservation и memory efficiency

#### 3.4 Agent Evaluation (обязательно)
**ADK Eval Framework** (`tests/agents/eval/`):
- 100% coverage всех агентов
- Метрики: `tool_trajectory_avg_score=1.0`, `pass_rate=100%`
- Запуск: `make eval`

Включить таблицу с метриками (запустить `make eval` и получить таблицу).

### 4. Demo & Validation (5-10%)
**Quick Start:** `make demo` — демонстрация без Telegram. **Production:** `make bot` — Telegram Bot для real-world usage. Пояснить зачем это нужно и почему важно делать make demo, когда есть make eval.

### 5. Technical Stack (2%)
Python 3.10+, Google ADK, Gemini 2.5 Flash, SQLite, python-telegram-bot. ОЧень краткое пояснить такой выбор технологий.

## Требования к Написанию

### Tone & Детализация
**Tone:** Технический презентационный (как tech pitch для инвесторов с engineering background). Избегать академизма.

**70% = Implementation. Объяснять:**
- **Почему** (design decisions), не только **что**
- **Как** ADK применён в файлах/функциях. Почему выбор пал на Google ADK, какие ещё альтернативы существуют?
- **Какие** преимущества vs альтернативы

**Пример хорошего:** "Orchestrator использует `sub_agents` вместо keywords, что позволяет LLM анализировать intent. Критично для ambiguous requests — 'что обсуждали вчера?' может быть summary или search."
**Пример плохого:** "Orchestrator роутит запросы."

### Упоминание Файлов
- ✅ "Функция `run_agent_sync` в `ai_core/common/adk.py`"
- ❌ "Строки 93-124"
- ❌ "См. README.md" (нет ссылок на другие документы!)

## Процесс
1. Изучить документацию (см. выше)
2. Прочитать код
3. Написать `writeup.ru.md` (ведущий, 1400 слов макс)
4. Написать `writeup.en.md` (перевод, 1400 слов макс, идентичное содержание, фактически просто перевод `writeup.ru.md` с контролем, чтобы на английском было не больше 1400 слов)

## Checklist
- [ ] Русский ≤ 1400 слов
- [ ] Английский ≤ 1400 слов, идентичен русскому
- [ ] ≥ 3 ADK концепта: Multi-agent, Tools, Sessions, Evaluation
- [ ] 60-70% = technical implementation
- [ ] Design decisions объяснены (почему)
- [ ] Нет ссылок на другие файлы проекта
- [ ] Нет code snippets
- [ ] Tone: технический презентационный
