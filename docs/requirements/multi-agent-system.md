# Требования: Мультиагентная Система

> Продуктовая логика и пользовательские сценарии работы агентов.

## Цель

Создать умного агента-оркестратора, который понимает контекст чата и автоматически координирует работу специализированных агентов для обработки запросов пользователей.

---

## Принцип Работы

Все текстовые сообщения из Telegram чата попадают в **Агент-Оркестратор**. Он анализирует контекст и решает:
- Касается ли сообщение его?
- Какой специализированный агент должен обработать запрос?
- Нужно ли вообще реагировать?

### Обязательные Случаи Реакции

- ✅ Реплай (ответ) на сообщение оркестратора → **всегда обрабатывается**
- ✅ Упоминание (@mention) бота в Telegram → **всегда обрабатывается**
- ✅ Ключевые фразы ("суммаризируй", "что говорили про...", "покажи сообщения") → **обрабатывается**
- ✅ Команды `/ask` и `/summary` → **обрабатывается**

### Поток сообщений Telegram (MVP)

- Все сообщения группового чата отправляются ботом в API `POST /api/chat/message` с обязательными полями `chat_id`, `user_id`, `user_name`, `text`, `message_id`, `reply_to_message_id?`.
- Оркестратор остаётся единственной точкой принятия решений: он обрабатывает только случаи выше и может вернуть пустой ответ, если реакция не требуется.
- Изоляция по чатам: `chat_id` передаётся в каждый вызов агентов и tools; данные и ответы не смешиваются между чатами.
- Команды `/ask` и `/summary` допускается вызывать через совместимые эндпоинты, но они должны использовать тот же `chat_id` и соблюдать изоляцию.

---

## Специализированные Агенты

### 1. Агент-Оркестратор

**Роль**: Центральный координатор, получает ВСЕ сообщения из Telegram чата и решает, что с ними делать.

**Важно**: Команды `/ask` и `/summary` необязательны. Оркестратор понимает natural language:
- "Суммаризируй последние 10 сообщений" → Chat Summarizer
- "Что говорили про проект Alpha?" → QA Agent
- "Найди сообщения про встречу" → Chat Observer

**Входные данные**:
- Текст сообщения из Telegram
- ID чата (chat_id)
- ID пользователя
- Контекст (история диалога, сессия)

**Инструменты (Sub-Agents)**:
- Агент-Суммаризатор Чата
- Агент-Отвечатель на вопросы
- Агент-Наблюдатель

**Примеры работы**:

**Пример 1: Запоминание инструкции**
```
👤 Пользователь: "В следующий раз, когда в чате будет сообщение 'с', это означает сделать итоговое саммари"

🤖 Оркестратор: [сохраняет в память сессии]

👤 Пользователь: (через 10 минут) "с"

🤖 Оркестратор: [распознает инструкцию] → вызывает Агент-Суммаризатор
```

**Пример 2: Роутинг по контексту**
```
👤 Пользователь: "Что мы говорили про оплату в прошлом месяце?"

🤖 Оркестратор: [определяет: вопрос к БЗ] → вызывает QA Agent
```

**Пример 3: Игнорирование**
```
👤 Пользователь А: "Привет!"
👤 Пользователь Б: "Как дела?"

🤖 Оркестратор: [не реагирует]
```

---

### 2. Агент-Суммаризатор Чата

**Роль**: Создает саммари истории чата и возвращает его в Telegram.

**Входные данные**:
- Запрос от оркестратора
- ID чата

**Инструменты**:
- `fetch_messages` - получение сообщений из SQLite
- `simple_summarizer` (AgentTool) - генерация саммари

**Примеры**:

```
👤: "Суммаризируй последние 10 сообщений"

🤖 Chat Summarizer:
"📝 Краткое саммари:
- Обсуждение проекта Mesh Mind
- Планирование архитектуры
- Выбор технологий: Google ADK, ChromaDB
- Распределение задач"
```

**Автоматическая обработка пересланных сообщений**:

При пересылке (forward) сообщений бот **обязательно указывает** оригинального автора:

```
👤: [пересылает 3 сообщения от разных людей]

🤖 Chat Summarizer:
"📝 Саммари пересланных сообщений:
- Иван Петров: обсуждал график работ
- Мария Сидорова: уточняла бюджет
- Петр Васильев: предложил новый подход"
```

---

### 3. Агент-Отвечатель (QA Agent)

**Роль**: Отвечает на вопросы ТОЛЬКО на основе базы знаний.

**Входные данные**:
- Вопрос пользователя
- ID чата (для поиска только в своем контексте)

**Инструменты**:
- `search_knowledge_base` - поиск в VectorDB с фильтрацией по chat_id

**Принцип "Честного ответа"**:
- ✅ Есть ответ в БЗ → возвращает с источником
- ❌ Нет ответа → **честно говорит "я не знаю"**

**Примеры**:

```
👤: "Что говорили про проект Alpha?"

🤖 QA Agent:
"📝 По проекту Alpha:
- Запуск следующая неделя
- Бюджет: 50К евро
- Ответственный: Петр Иванов
- Риск: нехватка разработчиков

(источник: чат от 20.11.2024)"
```

```
👤: "Какой цвет любит Мария?"

🤖 QA Agent:
"❓ Я не знаю. В базе знаний нет информации об этом."
```

---

### 4. Агент-Наблюдатель (Chat Observer)

**Роль**: Поиск конкретных сообщений в истории чата.

**Входные данные**:
- Критерии поиска
- ID чата

**Инструменты**:
- `fetch_messages` - поиск в SQLite

**Примеры**:

```
👤: "Найди все сообщения от Петра за вчера"

🤖 Chat Observer:
"📝 Найдено 5 сообщений от Петра за вчера:
1. [10:15] Нужно обсудить архитектуру
2. [11:30] Встреча перенесена
...
```

---

## Изоляция по Чатам

**Критически важно**: Каждый чат видит **только свои данные**.

- Все документы в VectorDB содержат `chat_id` в метаданных
- Все tools фильтруют по `chat_id`
- Агенты не смешивают данные разных чатов

**Пример изоляции**:
```
Чат А: "Бюджет проекта 100К"
Чат Б: "Бюджет проекта 50К"

👤 в Чате А: "Какой бюджет?"
🤖: "100К" (видит только данные Чата А)

👤 в Чате Б: "Какой бюджет?"
🤖: "50К" (видит только данные Чата Б)
```

---

## Сессии и Память

**Сессия = chat_id**:
- Каждый Telegram чат = отдельная сессия
- InMemorySessionService хранит контекст диалога
- Память не пересекается между чатами

**Что хранится в памяти**:
- История последних N сообщений
- Пользовательские инструкции ("когда говорю 'с', делай саммари")
- Контекст текущего диалога

---

## Definition of Done

Система считается готовой, если:

**Orchestrator:**
- ✅ Понимает natural language (без команд)
- ✅ Правильно роутит на sub-agents (eval score > 0.7)
- ✅ Сохраняет контекст в сессии
- ✅ Игнорирует нерелевантные сообщения

**Chat Summarizer:**
- ✅ Получает сообщения из SQLite
- ✅ Генерирует структурированное саммари
- ✅ При пересылке указывает оригинальных авторов

**QA Agent:**
- ✅ Отвечает на основе БЗ
- ✅ Честно говорит "я не знаю" (10-15% случаев)
- ✅ Указывает источники

**Chat Observer:**
- ✅ Находит сообщения по критериям
- ✅ Фильтрует по автору/дате

**Изоляция:**
- ✅ Все tools фильтруют по chat_id
- ✅ Нет утечки данных между чатами
- ✅ Тесты подтверждают изоляцию

---

## Ссылки

- [Технический Дизайн](../architecture/multi-agent-design.md) — схемы взаимодействия
- [Технические Требования](./technical-requirements.md) — детали реализации
- [Продуктовое Видение](./product-vision.md) — общая ценность продукта
