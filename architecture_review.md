# Архитектурный Ревью: Mesh Mind PoC

## Общая Оценка
Представленная архитектура выглядит **продуманной, современной и соответствующей целям MVP**. Разделение на компоненты (Bot, API, UI, CLI) обеспечивает хорошую модульность и возможность независимого развития частей системы. Выбор стека (Python, FastAPI, Streamlit, ChromaDB, Gemini) оптимален для быстрого прототипирования с мощными возможностями AI.

Однако, есть несколько нестыковок между продуктовыми требованиями и спецификациями, а также места, где можно упростить реализацию (KISS/YAGNI).

---

## Что сделано хорошо
1.  **Модульность**: Четкое разделение ответственности. API как единая точка входа для всех клиентов (Bot, CLI, UI) — это отличное решение, гарантирующее, что логика обработки данных (ингестия, RAG) не дублируется.
2.  **Выбор технологий**:
    *   **FastAPI + Pydantic**: Стандарт де-факто, обеспечивает типизацию и валидацию "из коробки".
    *   **Typer**: Отличный выбор для CLI.
    *   **ChromaDB**: Простая и эффективная локальная векторная БД, идеально для PoC.
    *   **Gemini Flash/Pro**: Грамотное разделение моделей на "быструю" (транскрибация, простые задачи) и "умную" (саммари, QA).
3.  **Мультимодальность**: Использование нативных возможностей Gemini для транскрибации аудио — это сильно упрощает архитектуру (не нужно тянуть Whisper и torch локально).
4.  **Тестирование**: Наличие разделов "Verification & Definition of Done" с конкретными ручными тестами в каждой спецификации — большой плюс.

---

## Что нужно улучшить

### 🔴 Критично (Нестыковки и Риски)
1.  **Обработка Изображений (Противоречие)**:
    *   `product_logic.md` (1.1): "Изображения: сохраняются как есть в БД".
    *   `specs/09_telegram_bot.md` (2.2): "Images: Ignore or reply 'Images are not supported yet'".
    *   **Рекомендация**: Для MVP лучше следовать спецификации бота (игнорировать или сохранять только file_id/путь без обработки), чтобы не усложнять. Но если продуктовое требование жесткое — нужно обновить спецификацию бота. *Советую пока сохранять их на диск (как файлы), но не обрабатывать, чтобы не потерять данные.*

2.  **Google ADK (`google.adk`)**:
    *   В спецификациях (07, 08) упоминается `google.adk.agents.llm_agent.Agent`. Если это внутренняя или специфическая библиотека, убедитесь, что она доступна и стабильна.
    *   **Рекомендация**: Если жесткой привязки нет, для PoC проще и надежнее использовать стандартный `google-generativeai` SDK. Это уберет лишнюю абстракцию и зависимость.

3.  **Идентификация Пользователей**:
    *   В системе есть "авторы писем" (email) и "пользователи Telegram" (chat_id/username). Сейчас нет механизма их склейки.
    *   **Риск**: Бот не поймет, что сообщение от `ivan_telega` и письмо от `ivan@company.com` — это один человек.
    *   **Рекомендация**: Для MVP забить (считать разными сущностями), но в модели данных `author_id` стоит предусмотреть поле `author_type` или таблицу `users` для будущего маппинга.

### 🟡 Не критично (Упрощения и Оптимизации)
1.  **База Данных (SQLAlchemy + Alembic)**:
    *   Для PoC использование `alembic` может быть overkill (лишний бойлерплейт с миграциями).
    *   **Рекомендация**: Рассмотрите **SQLModel**. Это обертка над SQLAlchemy и Pydantic от создателя FastAPI. Она идеально ложится в этот стек, сокращает код моделей в 2 раза и упрощает работу с БД.

2.  **Streamlit и API**:
    *   UI ходит в API по HTTP (`requests`). Это правильно архитектурно, но для локального PoC добавляет задержки и необходимость запускать два процесса.
    *   **Рекомендация**: Оставить как есть (это чище), но предусмотреть скрипт-пускач (например, в `Makefile` или `main.py`), который стартует и API, и UI одной командой.

3.  **Чанкинг (LangChain)**:
    *   В спецификации (06) упоминается `langchain-text-splitters` или кастом.
    *   **Рекомендация**: Если LangChain больше нигде не используется, не тяните его ради одного сплиттера. Напишите простую функцию на 20 строк (split by `\n\n`, then by `. `), это сэкономит сотни мегабайт зависимостей.

---

## Дополнительные Советы

1.  **Контекстное Окно**:
    *   В спецификации (07) предлагается обрезать старые сообщения. Gemini 1.5 Pro имеет контекст 1-2 млн токенов.
    *   **Совет**: Не тратьте время на сложную логику обрезки для MVP. Просто скармливайте всё (или последние 1000 сообщений). Это сработает лучше и проще.

2.  **Обработка Ошибок API**:
    *   Gemini API может давать `429 Too Many Requests` или `500`.
    *   **Совет**: Обязательно добавьте простой декоратор `@retry` (из библиотеки `tenacity`) на вызовы к LLM. Это критично для стабильности демо.

3.  **Безопасность API Key**:
    *   Убедитесь, что `.env` добавлен в `.gitignore`. Это классическая ошибка.

4.  **Единый Docker Compose (на будущее)**:
    *   Хоть сейчас "No Docker", но если понадобится поднять это на сервере, наличие `Dockerfile` для API сильно упростит жизнь. Но пока следуем правилу "No Docker".

## Итог
Архитектура принята. Рекомендую начать с **Common Lib** и **Storage**, затем **API**, и только потом нанизывать **Bot** и **UI**.
